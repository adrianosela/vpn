# vpn

#### Prerequisistes:

You need [Go](https://golang.org/dl/) for your operating system

#### Build binary:

```
go build
```

#### Run binary:

```
./vpn -uiport ${UI_PORT}
```
where UI_PORT is any unused TCP port